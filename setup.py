from setuptools import setup

# lint_dependencies = ["flake8", "flake8-docstrings", "black", "isort"]


setup(
    use_scm_version=True
    #####
    # TODO integrate into setup.cfg
    # url="https://github.com/MikeWooster/api-client",
    # include_package_data=True,
    # zip_safe=False,
)
